version: '3'

services:
  app:
    image: marcuslind90/digitalocean-terraform-docker:662f77a4
    restart: always

  nginx:
    image: nginx:1.15
    restart: always
    volumes:
      - ./resources/nginx/conf.d:/etc/nginx/conf.d
    ports:
      - "80:80"
