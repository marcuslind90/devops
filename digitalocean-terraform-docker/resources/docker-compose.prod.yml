version: '3'

services:
  app:
    image: marcuslind90/digitalocean-terraform-docker:a81a78cb
    restart: always

  nginx:
    image: nginx:1.15
    restart: always
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
    ports:
      - "80:80"
