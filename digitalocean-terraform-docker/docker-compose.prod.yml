version: '3'

services:
  app:
    image: marcuslind90/digitalocean-terraform-docker:4136c790
    restart: always

  nginx:
    image: nginx:1.15
    restart: always
    volumes:
      - ./resources/nginx/conf.d:/etc/nginx/conf.d
    ports:
      - "80:80"
